const title = document.getElementById("title");

title.id = "Id by JS"
title.innerText = "Inner Text by JS";
title.className = "ClassName by JS"
console.log(title.id);
console.log(title.innerText);
console.log(title.className);